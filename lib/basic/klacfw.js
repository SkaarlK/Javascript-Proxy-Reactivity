let 
    price = 5,
    quantity = 2,
    total = 0,
    target = null,
    storage = []

target = () => total = price * quantity

const record = (target) => storage.push(target)
const replay = () => storage.forEach(run => run())

